DATABASE_URL=postgres://daysout:change-me@db:5432/daysout
POSTGRES_PASSWORD=change-me

# Generate with: openssl rand -base64 32
AUTH_SECRET=generate-with-openssl-rand-base64-32

AUTH_URL=http://localhost:3000

# Generate with: node -e "const b=require('bcryptjs');b.hash('yourpin',12).then(h=>console.log(h))"
FAMILY_PIN_HASH=generate-with-bcrypt

# Generate with bcryptjs (admin password hash, set in Initiative 2)
ADMIN_PASSWORD_HASH=generate-with-bcrypt

LOG_LEVEL=info

# Groq API key — get free key at console.groq.com (used for URL import in Initiative 3)
GROQ_API_KEY=
